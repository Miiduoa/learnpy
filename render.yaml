services:
  - type: web
    name: python-learning-tutor
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn web_tutor.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0



